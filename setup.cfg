[tool:pytest]
# Keep default pytest invocation compatible with minimal environments where
# pytest-cov might not be installed (e.g. local pre-merge checks).
# Run coverage explicitly when the plugin is available:
#   pytest --cov=mvp --cov=basurin_io --cov-report=term-missing:skip-covered
addopts =
