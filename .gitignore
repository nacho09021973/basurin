__pycache__/
*.pyc
.pytest_cache/

# Local env + runtime artifacts
.venv/
runs/
runs_tmp/

# Local external data (H5, downloads)
external/

# Local datasets (do not commit)
data/

# local scratch / backups
local_backup/

# local
/docs/ringdown/window_catalog_v1.json
events_6.txt

# Scratch local (no canónico)
scratch/

# Cache local LOSC (no canónica)
data/losc/

# pytest-cov coverage artifacts
.coverage
.coverage.*
htmlcov/

# Local large downloads (Zenodo / data releases)
*.tar.gz
*.tgz
*.tar

# local data (never commit)
gw_data_32s/

# local GW cache (never commit)
gw_data_32s/

# Datos locales no versionables
gw_events/
